
// fun ir_params(x, y int) int {
//   x * y
// }

// fun ir_redundant_local(x, y int) int {
//   var b = y // storage unused
//   x * b     // b just points to y, not local storage
// }

// fun ir_if_then_ret(x, y int) int {
//   if x > y {
//     return x - y
//   }
//   x + y
// }

fun ir_if_else(x, y int) int {
  if x > y {
    x - y
  } else {
    x + y
  }
  // 3
}
